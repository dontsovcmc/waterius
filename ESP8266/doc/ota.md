# OTA обновление прошивки Waterius

OTA (Over-The-Air) позволяет обновлять прошивку и файловую систему Waterius удалённо, без физического подключения по USB.

**Поддерживаемые модели:** только Waterius-2 (MODEL_2, ESP-12F, flash 4MB).
ESP-01 (1MB flash) не поддерживает OTA — недостаточно места для staging area.

## Общая схема работы

```
ESP ──POST данные──> Сервер
ESP <──JSON ответ─── Сервер  (содержит секцию "ota" с url/md5/size файлов)
ESP ──GET──────────> filesystem.bin  (LittleFS, если есть в ota)
ESP ──GET──────────> firmware.bin
ESP ──перезагрузка──> Новая прошивка работает
```

Сервер решает, кому и когда отдавать OTA — ESP просто выполняет команду. Отдельного манифеста нет — OTA-данные передаются прямо в JSON-ответе сервера, что экономит один HTTP-запрос и ~15 КБ RAM.

## Последовательность работы

### 1. Штатный цикл отправки данных

ESP просыпается (по таймеру или кнопке), подключается к WiFi и отправляет показания на сервер. В JSON-запросе всегда есть поле `version_esp` с текущей версией прошивки.

### 2. Сервер отвечает с командой OTA

Если сервер решает что устройству нужно обновление, он включает в ответ секцию `ota`:

```json
{
  "ota": {
    "firmware": {
      "url": "https://cloud.waterius.ru/ota/nodemcuv2-2.0.25.bin",
      "size": 633344,
      "md5": "f14e1220b1565f63a4a69b84cab4582d"
    },
    "filesystem": {
      "url": "https://cloud.waterius.ru/ota/nodemcuv2-2.0.25-fs.bin",
      "size": 1024000,
      "md5": "6c023b5558cc375843449565ff0f2570"
    }
  }
}
```

Если обновление не нужно — пустой ответ `{}`.

Секция `ota` должна содержать хотя бы одну из подсекций (`firmware` или `filesystem`). Каждая подсекция содержит:

| Поле | Описание |
|------|----------|
| `url` | Полный HTTPS URL файла |
| `size` | Размер в байтах |
| `md5` | MD5-хеш файла |

### 3. Проверки перед обновлением

**Проверка батареи (3 замера, среднее):**

Перед OTA выполняются 3 измерения напряжения с интервалом 100мс. Пауза между замерами позволяет напряжению стабилизироваться после кратковременных просадок от WiFi-передачи. Среднее из 3 замеров сглаживает шум АЦП ESP8266 (±30-50 мВ).

| Проверка | Порог | Назначение |
|----------|-------|------------|
| Среднее > 4600 мВ | — | USB-питание, проверка пропускается |
| Среднее < 3300 мВ | Абсолютный порог | Минимум для 3×AA (3 × 1.1В) |

Порог 3300 мВ — надёжный минимум для любых типов батарей (Alkaline, NiMH) и АКБ.

**Порог USB-питания — 4600 мВ** (не 4900). Под нагрузкой WiFi напряжение USB проседает до ~4830 мВ. Свежие 3xAA дают до ~4800 мВ, но при WiFi нагрузке падают до ~4700 мВ, что всё ещё > 4600.

**Валидация OTA-данных:**
- Секция `ota` должна быть JSON-объектом
- Должна содержать хотя бы одну подсекцию (`firmware` или `filesystem`)
- Каждая подсекция должна содержать `url` и `md5`

**Проверка MD5:**
- `ESP8266httpUpdate` проверяет MD5 после скачивания, до записи во flash
- Если MD5 не совпадает — файл отбрасывается, старая прошивка не тронута

### 4. Обновление файловой системы

Если в `ota` есть секция `filesystem`:
- Вызывается `extendWakeUp()` — сброс таймера Attiny85 (120 секунд)
- `ESPhttpUpdate.updateFS()` скачивает и записывает LittleFS-образ
- Перезагрузка отключена (`rebootOnUpdate(false)`) — после FS нужно ещё обновить firmware

### 5. Обновление прошивки

Если в `ota` есть секция `firmware`:
- Вызывается `extendWakeUp()`
- `ESPhttpUpdate.update()` скачивает прошивку в staging area
- Перезагрузка отключена — после записи выполняется явная проверка результата

### 6. Перезагрузка и проверка

После успешного обновления всех компонентов:
```
OTA: filesystem updated OK
OTA: firmware updated OK
OTA: complete, restarting ESP...
```
ESP вызывает `ESP.restart()`. Bootloader (eboot) копирует прошивку из staging area в APP-область (~3.8 секунды для 633 КБ).

После перезагрузки ESP проходит обычный цикл: WiFi → NTP → отправка данных. В JSON уже новая `version_esp`. Сервер видит новую версию и отвечает `{}` — OTA больше не запускается. Если OTA завершилось ошибкой, статус ошибки (`ota_error`) передастся серверу при следующем штатном пробуждении.

## Коды ошибок OTA

Код ошибки сохраняется в `Settings.reserved9[0]` (EEPROM). При следующей отправке данных включается в JSON как `ota_error`. После успешной отправки обнуляется.

| Код | Константа | Описание |
|-----|-----------|----------|
| 0 | `OTA_ERR_NONE` | Нет ошибки |
| 1 | `OTA_ERR_PARSE` | Ошибка данных (нет url/md5, пустая секция ota) |
| 2 | `OTA_ERR_FS_UPDATE` | Ошибка обновления filesystem (MD5, обрыв, нет связи и т.д.) |
| 3 | `OTA_ERR_FW_UPDATE` | Ошибка обновления firmware (MD5, обрыв, нет связи и т.д.) |
| 4 | `OTA_ERR_LOW_BATTERY` | Батарея разряжена (напряжение < 3300 мВ) |

Счётчик попыток OTA реализуется на стороне сервера: сервер видит `ota_error` и `version_esp` в каждом запросе и может прекратить отдавать OTA после N ошибок.

## Настройка сервера

### Python Flask (пример)

Минимальный сервер для управления раздачей OTA:

```python
from flask import Flask, request, jsonify

app = Flask(__name__)

OTA_ENABLED = True
OTA_TARGET_VERSION = "2.0.24"  # OTA только для этой версии ("" = для всех)
OTA_CONFIG = {
    "firmware": {
        "url": "https://cloud.waterius.ru/ota/nodemcuv2-2.0.25.bin",
        "size": 637200,
        "md5": "b66e9d1297bc1a170d140aeddc3e15b8"
    },
    "filesystem": {
        "url": "https://cloud.waterius.ru/ota/nodemcuv2-2.0.25-fs.bin",
        "size": 1024000,
        "md5": "6c023b5558cc375843449565ff0f2570"
    }
}

@app.route("/cloud-waterius-ru", methods=["POST"])
def waterius():
    data = request.get_json(silent=True) or {}
    version_esp = data.get("version_esp", "")

    response = {}
    if OTA_ENABLED:
        if OTA_TARGET_VERSION == "" or version_esp == OTA_TARGET_VERSION:
            response["ota"] = OTA_CONFIG

    return jsonify(response)
```

**Параметры:**

| Параметр | Описание |
|----------|----------|
| `OTA_ENABLED` | `True` — отдавать OTA, `False` — обычный ответ `{}` |
| `OTA_TARGET_VERSION` | Версия ESP для обновления. `"2.0.24"` — только 2.0.24 получит OTA. `""` — все версии |
| `OTA_CONFIG` | Секция `ota` с url/md5/size файлов |

**Защита от зацикливания:** сервер проверяет `version_esp` из POST-данных. Если версия ESP уже обновлена — `ota` не отдаётся.

### Настройка release-сервера

1. Создать директорию для OTA-файлов на сервере (например `/ota/`)
2. Загрузить файлы:
   - `nodemcuv2-X.X.X.bin` (firmware)
   - `nodemcuv2-X.X.X-fs.bin` (filesystem)
3. Убедиться что файлы доступны по HTTPS
4. Указать url/md5/size каждого файла в конфигурации сервера
5. Установить `OTA_TARGET_VERSION` на версию, которую нужно обновить
6. Включить `OTA_ENABLED = True`

### Отключение OTA после обновления

Два способа:
- **Автоматический:** `OTA_TARGET_VERSION = "2.0.24"` — после обновления до 2.0.25 устройство перестанет получать OTA
- **Ручной:** `OTA_ENABLED = False` — полностью отключить OTA для всех

## Сборка OTA-бинарей

### Скрипт сборки

```bash
cd ESP8266
./scripts/build_and_deploy.sh 2.0.25
```

Скрипт выполняет:
1. Временно подменяет `firmware_version` в `platformio.ini` на указанную версию
2. Собирает прошивку: `platformio run --environment waterius_2`
3. Собирает файловую систему: `platformio run --target buildfs --environment waterius_2`
4. Вычисляет MD5 и размеры
5. Копирует бинари в `ota/`
6. Выводит JSON-фрагмент секции `ota` для вставки в конфигурацию сервера
7. Восстанавливает `platformio.ini` (через trap, даже при ошибке)

Результат:
```
ESP8266/ota/
├── nodemcuv2-2.0.25.bin        # firmware (~633 KB)
└── nodemcuv2-2.0.25-fs.bin     # filesystem (~1000 KB)
```

### VSCode Task

В `.vscode/tasks.json` есть задача **OTA: Build and prepare**. При запуске запрашивает версию (по умолчанию 2.0.25).

### Переключение debug / release

В `scripts/build_and_deploy.sh` изменить `OTA_BASE_URL`:

```bash
# Debug
OTA_BASE_URL="https://us.pstd.ru/us-files/waterius"

# Release
OTA_BASE_URL="https://cloud.waterius.ru/ota"
```

### Загрузка на сервер

После сборки загрузить содержимое `ota/` на сервер. Файлы должны быть доступны по HTTPS.

## Тайминги

Реальные замеры на Waterius-2 (ChipId: 682bdb), WiFi 802.11n, HTTPS, сервер us.pstd.ru (2026-03-16):

### Полный цикл с OTA (FS + firmware)

```
00:00  ESP проснулся
00:04  WiFi подключён                         (~3.8с)
00:04  NTP синхронизация                       (~0.05с)
00:05  Отправка данных #1                      (~1.1с)
00:05  Сервер вернул ota
00:06  apply_settings + отправка данных #2     (~1с)
00:06  OTA: start
00:06  Проверка батареи (USB: 4732 mV)         пропущена
00:07  OTA: downloading filesystem...
00:54  OTA: filesystem updated OK              (~47с, 1024 КБ)
00:54  OTA: downloading firmware...
01:28  OTA: firmware updated OK                (~34с, 633 КБ)
01:28  OTA: complete, restarting ESP...
       ──── перезагрузка (eboot ~3.8с) ────
03:50  Новая прошивка стартовала
07:45  Firmware ver: 2.0.25
09:06  Going to sleep
```

### Тайминги по этапам

| Этап | Время | Размер | Скорость |
|------|-------|--------|----------|
| WiFi подключение | ~3.8с | — | — |
| NTP синхронизация | ~0.05с | — | — |
| Отправка данных | ~1.1с | ~900 Б | — |
| Проверка батареи | ~0.2с | — | — |
| Обновление filesystem | ~47с | 1024 КБ | ~22 КБ/с |
| Обновление firmware | ~34с | 633 КБ | ~19 КБ/с |
| Перезагрузка (eboot copy) | ~3.8с | 633 КБ | — |
| **Полный цикл с OTA** | **~88с** | — | — |
| **Штатный цикл без OTA** | **~5-9с** | — | — |

Скорость скачивания ~19-22 КБ/с (HTTPS через WiFi 802.11n). Запас по таймеру Attiny: `extendWakeUp()` даёт 120 секунд, OTA занимает ~82 секунды — резерв ~38 секунд.

## Flash layout (ESP8266, 4MB)

```
┌──────────────────────────────────────┐
│ Bootloader (eboot): 4 KB            │ 0x000000
├──────────────────────────────────────┤
│ APP (текущая прошивка): ~633 KB      │ 0x001000
├──────────────────────────────────────┤
│ Staging area (свободно): ~2.4 MB     │ ← сюда скачивается новая прошивка
├──────────────────────────────────────┤
│ LittleFS: 1 MB                      │ 0x300000
├──────────────────────────────────────┤
│ EEPROM + WiFi config: 20 KB         │ 0x3FB000
└──────────────────────────────────────┘
```

ESP8266 **не имеет** dual-partition OTA (A/B) как ESP32. Используется staging area:

1. `ESP8266httpUpdate` скачивает новую прошивку в staging area
2. MD5 проверяется до записи команды bootloader
3. Если MD5 совпал — записывается команда для eboot в RTC-память
4. ESP перезагружается
5. eboot копирует прошивку из staging area в APP-область
6. Загружается новая прошивка

**Безопасность:**
- Если скачивание прервалось или MD5 не совпал — старая прошивка не тронута
- Единственный риск — сбой питания во время копирования eboot (шаг 5, ~1-2 сек). `extendWakeUp()` гарантирует что Attiny не отключит питание
- Обе копии (текущая + staging) помещаются между bootloader и LittleFS (~3 МБ, при прошивке ~633 КБ запас огромный)

**Обновление LittleFS:**
- `updateFS()` записывает образ напрямую в область LittleFS (0x300000)
- Не использует staging area — прямая запись

## Ограничения и риски

### Нет rollback

Если новая прошивка загрузилась но не работает — откат только через USB. Тестируйте прошивку перед публикацией на сервер.

### Только Waterius-2

OTA компилируется только для `WATERIUS_MODEL == WATERIUS_MODEL_2` (ESP-12F, 4MB flash). На ESP-01 (1MB) не хватает места для staging area.

### Расход батареи

OTA потребляет больше энергии: WiFi активен ~67 секунд вместо обычных ~5-9. Одно обновление не критично для 3xAA батарей, но не стоит запускать OTA часто.

### HTTPS без проверки сертификата

Используется `setInsecure()` (как и во всём проекте). Трафик шифруется, но сертификат сервера не проверяется. Для OTA это допустимо благодаря MD5-проверке содержимого.

### Обновление Attiny85 невозможно

ESP подключён к Attiny85 только по I2C — нет доступа к ISP-пинам. Обновление Attiny только через USB-программатор.

### Настройки после OTA

EEPROM (Settings) сохраняется при OTA — настройки WiFi, показания счётчиков и все пользовательские параметры не теряются. Настройки сбросятся только если в новой прошивке изменилась `CURRENT_VERSION` конфигурации.

## Настройка сервера (WordPress + Code Snippets)

Файл `doc/waterius_ota_snippet.php` — готовый PHP-сниппет для плагина Code Snippets (WordPress).

Установка:
1. Установить плагин Code Snippets на WordPress
2. Создать новый PHP-сниппет, вставить содержимое `waterius_ota_snippet.php`
3. В настройках Waterius (captive portal) указать HTTP URL: `https://your-server.com/cloud-waterius-ru`
4. Для управления OTA менять `$OTA_ENABLED` и `$OTA_TARGET_VERSION` в сниппете

Настройки в сниппете:
- `$OTA_ENABLED` — `true` для раздачи OTA, `false` для отключения
- `$OTA_TARGET_VERSION` — версия ESP для обновления (`''` = все версии)
- `$OTA_CONFIG` — секция `ota` с url/md5/size (генерируется скриптом `build_and_deploy.sh`)

## Структура файлов

```
ESP8266/
├── src/
│   ├── ota_parse.h            # Парсинг OTA JSON (parse_ota_params(), struct OtaParams, коды ошибок)
│   ├── ota_update.h           # Объявление perform_ota_update(), пороги напряжения
│   ├── ota_update.cpp         # Реализация OTA (проверка батареи, скачивание, запись, рестарт)
│   ├── main.cpp               # Вызов OTA после получения секции "ota" от сервера
│   └── json.cpp               # Отправка ota_error в JSON
├── test/
│   └── test_ota/
│       ├── main.cpp                 # Точка входа googletest
│       └── test_url_validation.cpp  # 11 тестов парсинга OTA JSON (native, googletest)
├── scripts/
│   └── build_and_deploy.sh    # Скрипт сборки OTA-бинарей
├── ota/                       # Результат сборки (не в git)
│   ├── nodemcuv2-X.X.X.bin
│   └── nodemcuv2-X.X.X-fs.bin
├── doc/
│   ├── ota.md                 # Этот документ
│   └── waterius_ota_snippet.php  # PHP-сниппет для WordPress Code Snippets
└── plan/
    └── ota_v2_plan.md         # План и результаты тестирования
```

## Запуск тестов

```bash
cd ESP8266
platformio test --environment native
```

11 тестов (googletest) покрывают парсинг OTA JSON:
- Полный OTA (firmware + filesystem), только firmware, только filesystem
- Пустой объект, отсутствующие url/md5, firmware не объект
- Значение size по умолчанию (0), проверка кодов ошибок
